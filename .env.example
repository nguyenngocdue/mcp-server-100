PORT=3000
API_KEY=secret-123
NODE_ENV=development

# Bearer token cho MCP chuẩn (streamable-http)
MCP_TOKEN=sk-123
#docker run -d -p 3000:3000 --name mcp-server-test -e NODE_ENV=production -e MCP_TOKEN=sk-test-token -e API_KEY=test-api-key mcp-server-100