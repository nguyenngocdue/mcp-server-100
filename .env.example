# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication
API_KEY=your-api-key-here
MCP_TOKEN=sk-your-mcp-token-here

# For Render deployment, these will be auto-generated
# Or set manually in Render dashboard:
# - NODE_ENV=production
# - PORT=auto (managed by Render)
# - API_KEY=secure-random-key
# - MCP_TOKEN=sk-secure-random-token

# Docker run example:
# docker run -d -p 3000:3000 --env-file .env mcp-server-100